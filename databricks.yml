# Deploy: see README.md (databricks bundle deploy + DATABRICKS_BUNDLE_ENGINE=direct)
bundle:
  name: kafka-lakeflow-orders

workspace:
  host:    https://adb-1952652121322753.13.azuredatabricks.net
  profile: biju
  root_path: ~/training/v2-training/kafka-lakeflow

include:
  - resources/*.yml

targets:
  dev:
    mode:    development
    default: true

  prod:
    mode: production
    workspace:
      root_path: ~/training/v2-training/kafka-lakeflow
